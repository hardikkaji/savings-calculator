import type { Config } from "@react-router/dev/config";

export default {
  // Config options...
  // To enable SPA mode set this to `false`
  // See: https://reactrouter.com/en/main/route/route-config#ssr
  ssr: false,
} satisfies Config;
